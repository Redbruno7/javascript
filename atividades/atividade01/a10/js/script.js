let base = Number(prompt('Insira a base do retângulo: '));
let altura = Number(prompt('Insira a altura do retângulo: '));
let perimetro = 2 * (base + altura);

console.log('O perímetro deste retângulo é:', perimetro.toFixed(2));