let base = Number(prompt('Insira a base do triângulo: '));
let altura = Number(prompt('Insira a altura do triângulo: '));
let area = (base * altura) / 2;

console.log('A área deste retângulo é:', area);